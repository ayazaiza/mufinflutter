import 'package:flutter/material.dart';

class BottomNavPage {
  String label;
  Widget activeIcon;
  Widget icon;
  BottomNavPage(
      {required this.label, required this.activeIcon, required this.icon});
}
